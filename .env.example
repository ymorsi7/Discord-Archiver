# Create .env and add your bot token (Discord Developer Portal → Bot → Reset Token / Copy)
DISCORD_TOKEN=your_bot_token_here

# Optional: invite link shown on https://your-app.fly.dev/ (OAuth2 URL Generator in Discord Developer Portal)
# INVITE_URL=https://discord.com/oauth2/authorize?client_id=...

# Optional: default folder for archives (you can also pass a path when running !archive).
OUTPUT_DIR=./archive
